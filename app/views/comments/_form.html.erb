<%= form_for([@apartmentsw,@apartmentsw.comments.build]) do |f| %>
    <p>
        <%= f.label :username %>
        <%= f.text_area :body %>
    </p>
    <p>
        <%= f.label :body %><br>
        <%= f.text_area :body %>
        
    </p>
    <br>
    
    <p>
        <%= f.submit %>
    </p>
<% end %>